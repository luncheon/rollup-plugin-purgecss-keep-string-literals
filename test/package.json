{
  "private": true,
  "scripts": {
    "test": "rimraf dist/ && rollup -c --silent && node src/check.js && rimraf dist/"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^6.1.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.33.3",
    "tslib": "^2.0.3",
    "typescript": "^4.1.2"
  }
}
